# ⭐Productivity Hub

A minimal,**productivity hub** built with plain **HTML, CSS, JS**.

## 🧰 What’s Inside

- 👤 **Portfolio Card**
  - Name, title,
  - Avatar with initial
  - Social icons (GitHub, Instagram, LinkedIn, Google Keep)

- 📂 **Productivity Hub (Tabs)**
  - ✅ **Todos** – add simple tasks
  - 📝 **Notes** – title + content
  - 🔗 **Links** – save URLs with optional title
  - 🖼️ **Images** – save image URLs with optional caption

- 🌙 **Theme Toggle**
  - Button (`☀`) to switch theme (handled via `script.js` + `style.css`)

---

## ⚙️ Setup

. Put files together:

   ```text
   .
   ├── index.html
   ├── style.css
   └── script.js
